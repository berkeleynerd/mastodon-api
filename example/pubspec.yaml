name: mastodon_api_example
description: Example for the Mastodon API Dart library.
publish_to: 'none'

environment:
  sdk: ^3.7.2

dependencies:
  mastodon_api:
    path: ../
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  webview_flutter: ^4.2.0
  oauth2: ^2.0.3
  http: ^1.1.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
flutter:
  uses-material-design: true 